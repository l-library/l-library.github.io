<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>我在互联网造文物？“赛博做旧”踩坑记录</title>
      <link href="/2026/03/26/%E6%88%91%E5%9C%A8%E4%BA%92%E8%81%94%E7%BD%91%E9%80%A0%E6%96%87%E7%89%A9%EF%BC%9F%E2%80%9C%E8%B5%9B%E5%8D%9A%E5%81%9A%E6%97%A7%E2%80%9D%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/"/>
      <url>/2026/03/26/%E6%88%91%E5%9C%A8%E4%BA%92%E8%81%94%E7%BD%91%E9%80%A0%E6%96%87%E7%89%A9%EF%BC%9F%E2%80%9C%E8%B5%9B%E5%8D%9A%E5%81%9A%E6%97%A7%E2%80%9D%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<p>最近边学Qt边做了一个号称 <a href="https://github.com/l-library/MemeGenerator">“表情包制作器”</a> 的图像处理类软件，结果花费了两个月还是只搭出来一个基本框架，只有破产版的图像处理功能，好在是能跑。<br><img src="/../asset/2026-3/1.png" alt="软件界面"><br>虽说有些功能还有一些问题，但是在那之前，得先做点东西告诉用户这个软件是用来做表情包的，那有什么是表情包特色呢？一拍脑袋，搞个“赛博做旧”的功能吧。</p><h2 id="电子包浆的特点"><a href="#电子包浆的特点" class="headerlink" title="电子包浆的特点"></a>电子包浆的特点</h2><p>要搞赛博做旧，首先要明确与之息息相关的<strong>电子包浆</strong>的概念。所谓电子包浆，就是指表情包经过广泛传播、被一个又一个平台反复压缩并刻上水印的现象。这样的表情包极有可能来自于互联网的早期时代，能够流传至今，说明其经过了千锤百炼，受到了人民大众的广泛喜爱，称得上经得起时间考验的true meme👍。<br>经过我的观察，一个表情包可以被称为电子包浆，不外乎几大特点：</p><ul><li>低到令人发指的分辨率</li><li>充满JPEG压缩的噪点</li><li>堪比陈年污垢厚度的水印</li><li>不知从何而来的聊天边框、点赞图标</li><li>奇形怪状的颜色（比如绿色）</li><li>某位先民留下的涂鸦</li><li>让人留下时代眼泪的古早梗</li></ul><p>以后如果你发现觉得哪个表情包显得很古老，可以套套上面的特点，总会命中一两个。</p><h2 id="实现方案"><a href="#实现方案" class="headerlink" title="实现方案"></a>实现方案</h2><p>上面提出的几大特点，古早梗方面需要用户发挥主观能动性，其余的都可以让软件来完成。就先从看起来比较简单的开始吧。</p><h3 id="对话框设计"><a href="#对话框设计" class="headerlink" title="对话框设计"></a>对话框设计</h3><p>窗口不需要太复杂，一个预览部分，一个调整参数的部分和应用、取消键即可。<br>按计划，参数应该包括以下几个方面：</p><ul><li>滑动条&amp;微调框<ul><li>分辨率</li><li>噪点数量</li><li>色彩失真</li><li>水印数量</li></ul></li><li>勾选/取消<ul><li>扫描线效果</li><li>意义不明的边框效果</li></ul></li></ul><p>最后实现出来的对话框效果大概是这样：<br><img src="/../asset/2026-3/2.png" alt="对话框"></p><h3 id="分辨率压缩"><a href="#分辨率压缩" class="headerlink" title="分辨率压缩"></a>分辨率压缩</h3><p>可以直接通过<strong>缩放QImage</strong>来实现分辨率压缩功能。</p><pre><code class="language-cpp">    qreal scale = (100.0 - m_resolution) / 100;    if (scale == 0) // 防止m_resolution=100的情况        scale = 0.001;    int newWidth = m_image.width() * scale;    int newHeight = m_image.height() * scale;    if (newWidth &gt; 0 &amp;&amp; newHeight &gt; 0)    {        m_baseImage = m_image.scaled(newWidth, newHeight, Qt::KeepAspectRatio, Qt::SmoothTransformation);    }</code></pre><p>一开始没有加上scale不为0的判断，导致滚动条拉到100的时候程序的内存占用直接干到了95%……加上判断后就没有问题了。</p><p><img src="/../asset/2026-3/3.png" alt="分辨率压缩"></p><h3 id="加入噪点"><a href="#加入噪点" class="headerlink" title="加入噪点"></a>加入噪点</h3><p>表情包里会出现的噪点现象主要是由多次的JPEG压缩引起的。引用一下百度百科的说法：</p><blockquote><p>由于JPEG格式的图像在缩小图像尺寸后图像仍显得很自然，因此就可以利用特殊的方法来减小图像数据。此时，它就会以上下左右8×8个像素为一个单位进行处理。因此尤其是在8×8个像素边缘的位置就会与下一个8×8个像素单位发生不自然的结合。<br>由JPEG格式压缩而产生的图像噪音也被称为马赛克噪音（Block Noise），压缩率越高，图像噪音就越明显。<br>虽然把图像缩小后这种噪音也会变得看不出来，但放大打印后，一进行色彩补偿就表现得非常明显。这种图像噪音可以通过利用尽可能高的画质或者利用JPEG格式以外的方法来记录图像而得以解决。</p></blockquote><p>我们要做的不是解决这个问题，而是制造这个问题。</p><p>既然噪点产生的原因找到了，接下来<strong>模拟JPEG压缩</strong>即可：</p><pre><code class="language-cpp">    QImage workImage = m_filteredImage;    int iterations = m_noise / 10 + 3;    int quality = qMax(5, 100 - m_noise);    for (int iter = 0; iter &lt; iterations; ++iter)    {        QBuffer buffer;        buffer.open(QIODevice::WriteOnly);        workImage.save(&amp;buffer, "JPEG", quality);        QImage compressed;        compressed.loadFromData(buffer.buffer(), "JPEG");        workImage = compressed.isNull() ? m_filteredImage : compressed;    }</code></pre><p>在QBuffer里将图片作为JPEG存入，再从中读出，多次重复这个操作，即可制造出以假乱真的噪点（也许就是真的？）。</p><p>保存的次数以及保存时的质量都由m_noise决定。效果大致是这样：</p><p><img src="/../asset/2026-3/7.png" alt="噪点"></p><h3 id="色彩失真"><a href="#色彩失真" class="headerlink" title="色彩失真"></a>色彩失真</h3><p>图像的色彩偏移效果大概可以分为四层：</p><ul><li>$8 \times 8$ 块级偏移，均匀偏移</li><li>$4 \times 4$ 块级偏移，分通道偏移</li><li>单像素级偏移，高对比区域</li><li>对角插值偏移，像素级微调</li></ul><p>其中的偏移量利用QRandomGenerator随机生成，m_color决定随机的范围。</p><pre><code class="language-cpp">// 8*8int offset = QRandomGenerator::global()-&gt;bounded(-intensity / 3, intensity / 3 + 1);// 4*4int rOffset = QRandomGenerator::global()-&gt;bounded(-intensity / 2, intensity / 2 + 1);int gOffset = QRandomGenerator::global()-&gt;bounded(-intensity / 2, intensity / 2 + 1);int bOffset = QRandomGenerator::global()-&gt;bounded(-intensity / 2, intensity / 2 + 1);</code></pre><p>分层、分通道地模拟色彩偏移效果。</p><pre><code class="language-cpp">    for (int y = 0; y &lt; height; y += 4)    {        for (int x = 0; x &lt; width; x += 4)        {            if (QRandomGenerator::global()-&gt;bounded(100) &lt; 15)            {                int rOffset = QRandomGenerator::global()-&gt;bounded(-intensity / 2, intensity / 2 + 1);                int gOffset = QRandomGenerator::global()-&gt;bounded(-intensity / 2, intensity / 2 + 1);                int bOffset = QRandomGenerator::global()-&gt;bounded(-intensity / 2, intensity / 2 + 1);                for (int by = 0; by &lt; 4 &amp;&amp; y + by &lt; height; ++by)                {                    for (int bx = 0; bx &lt; 4 &amp;&amp; x + bx &lt; width; ++bx)                    {                        QRgb pixel = result.pixel(x + bx, y + by);                        int r = qBound(0, qRed(pixel) + rOffset, 255);                        int g = qBound(0, qGreen(pixel) + gOffset, 255);                        int b = qBound(0, qBlue(pixel) + bOffset, 255);                        result.setPixel(x + bx, y + by, qRgb(r, g, b));                    }                }            }        }    }</code></pre><p><img src="/../asset/2026-3/4.png" alt="色彩偏移"></p><h3 id="扫描线效果"><a href="#扫描线效果" class="headerlink" title="扫描线效果"></a>扫描线效果</h3><p>扫描线的效果相对来说比较好实现，主要就是有间隔地在图片上绘制黑色横线。</p><pre><code class="language-cpp">QPainter painter(&amp;m_filteredImage);painter.setPen(Qt::NoPen);int height = m_filteredImage.height();for (int y = 0; y &lt; height; y += m_OutputSpacing) {    painter.setOpacity(0.15);    painter.setBrush(QColor(0, 0, 0));    painter.drawRect(0, y, m_filteredImage.width(), m_ScanLineHeight);}painter.end();</code></pre><p>这样的实现采用了固定的扫描线间距和扫描线宽度，这样会导致扫描线在预览中呈现的效果随图片大小的增大而越来越不明显，最后看起来好像只是让图片变灰了一点。<br><img src="/../asset/2026-3/5.png" alt="扫描线效果图"><br>考虑到一般表情包都不会以较大的形式呈现，过于密集的扫描线难以让人肉眼可见地看到变化，我们需要加入一个动态的调整机制，让扫描线的宽度和间距随着图片大小的变化而变化，也许不太符合实际扫描线的情况，但是对于本项目来说，视觉上的冲击力比拟真更重要。</p><pre><code class="language-cpp">QPainter painter(&amp;m_filteredImage);painter.setPen(Qt::NoPen);const int scanLineSpacingOnScreen = 3;QSize fixedSize(400, 300);QSize scaledSize = fixedSize.boundedTo(m_filteredImage.size());scaledSize.scale(fixedSize, Qt::KeepAspectRatio);double scaleX = static_cast&lt;double&gt;(m_filteredImage.width()) / scaledSize.width();double scaleY = static_cast&lt;double&gt;(m_filteredImage.height()) / scaledSize.height();double scale = qMax(scaleX, scaleY);int spacing = qMax(1, static_cast&lt;int&gt;(scanLineSpacingOnScreen * scale));int height = m_filteredImage.height();for (int y = 0; y &lt; height; y += spacing) {    painter.setOpacity(0.15);    painter.setBrush(QColor(0, 0, 0));    painter.drawRect(0, y, m_filteredImage.width(), qMax(1, spacing / 2));}painter.end();</code></pre><p>最终达到的效果大概是这样：<br><img src="/../asset/2026-3/6.png" alt="扫描线效果图2"></p><h3 id="添加水印"><a href="#添加水印" class="headerlink" title="添加水印"></a>添加水印</h3><p>首先要解决的问题就是水印里写什么，虽说只是气氛组，万一有人注意到水印里写的全是随机生成的汉字，未免有些破坏气氛。</p><p>所以随便找了些用户名存在UserName.json中，方便后续随时更改，并且在水印添加函数里随机读取：</p><pre><code class="language-cpp">void CyberDistressingDialog::applyWaterMark(){    // 打开配置文档    QFile file(":/config/UserName.json");    if (!file.open(QIODevice::ReadOnly))    {        qWarning() &lt;&lt; "无法打开用户名文件";        return;    }    QByteArray data = file.readAll();    file.close();    QJsonDocument doc = QJsonDocument::fromJson(data);    if (!doc.isArray())    {        qWarning() &lt;&lt; "文件不是数组形式";        return;    }    QJsonArray array = doc.array();    if (array.empty())    {        qWarning() &lt;&lt; "数组为空";        return;    }    // 随机选择用户名    for (int i = 0; i &lt;= m_watermark / 4; ++i)    {        int index = QRandomGenerator::global()-&gt;bounded(array.size());        drawWaterMark(array.at(index).toString());    }    need_to_change_mark = false;}</code></pre><p>之后，再把这些名字绘制在图中，这里的策略是在四个位置随机生成，并且水印的大小是随着图片大小的变化动态变化的：</p><pre><code class="language-cpp">switch (position)    {    case Center:        // 中央位置        drawPosition = QPoint(            (imageWidth - textWidth) / 2,            (imageHeight - textHeight) / 2);        break;    case BottomLeft:        // 左下位置        drawPosition = QPoint(            0,            imageHeight - textHeight / 2);        break;    case BottomRight:        // 右下位置        drawPosition = QPoint(            imageWidth - textWidth,            imageHeight - textHeight / 2);        break;    case BottomCenter:        // 中间最下位置        drawPosition = QPoint(            (imageWidth - textWidth) / 2,            imageHeight - textHeight / 2);        break;    }</code></pre><p>最终效果如下：</p><p><img src="/../asset/2026-3/8.png" alt="img"></p><h1 id="最终效果图"><a href="#最终效果图" class="headerlink" title="最终效果图"></a>最终效果图</h1><p><img src="/../asset/2026-3/9.jpeg" alt="原图"></p><p><img src="/../asset/2026-3/10.png" alt="修改后"></p>]]></content>
      
      
      
        <tags>
            
            <tag> 学习记录 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
